<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="15dp"
    tools:context="com.example.ankit.jolly.mobitar.MainActivity">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

    <RadioGroup xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="right">
        <RadioButton android:id="@+id/chordA"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:button="@drawable/button"
            android:onClick="onRadioButtonClicked"/>
        <RadioButton android:id="@+id/chordB"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:button="@drawable/button"
            android:onClick="onRadioButtonClicked"/>
        <RadioButton android:id="@+id/chordC"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:button="@drawable/button"
            android:onClick="onRadioButtonClicked"/>
        <RadioButton android:id="@+id/chordD"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:button="@drawable/button"
            android:onClick="onRadioButtonClicked"/>
    </RadioGroup>

    </LinearLayout>
</android.support.constraint.ConstraintLayout>
